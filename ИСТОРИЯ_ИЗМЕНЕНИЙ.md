# История изменений

## 2026-09-09 — Owner UX: самообслуживание поставщика (ЛС без SQL)

- Миграция `0006` (`supplier_bind_tokens`): одноразовые t.me-ссылки для привязки ЛС поставщика, TTL 24 ч, привязаны к `supplier_id` и owner.
- `/menu` → «Поставщики» → «Добавить поставщика»: после имени бот предлагает три способа завести ЛС:
  - **Переслать сообщение** — бот извлекает `telegram_id` из forward (как у сотрудников);
  - **Ссылка** — `https://t.me/<bot>?start=<token>`; поставщик нажимает Start и привязывается автоматически;
  - **Пропустить** — только групповой чат, ЛС не настроен.
- Карточка поставщика: кнопки **Привязать личку** / **Сменить личку** / **Отвязать личку**; статус Telegram ID и `dm_ok` видны. Отвязка не трогает групповой чат.
- `/start <token>` от неизвестного: не создаёт нового поставщика, не раскрывает чужие данные; при конфликте занятого `telegram_id` токен сжигается и owner уведомляется.
- Общий helper `resolve_user_id_from_message` устраняет дублирование парсеров между employee- и supplier-диалогами.
- Технарь больше не пишет `suppliers.telegram_id` в SQL для штатного сценария.

## 2026-09-09 — Owner UX: канал прайса, кнопки approve, Telegram ID поставщика (legacy)

- Миграции `0006` (`supplier_bind_tokens`) + `0007` (`approved_by_telegram_id`, fix partial unique default chat).
- `/menu` → карточка поставщика: привязка канала прайса (parser PK в callback, Telegram id в БД), задание/сброс `telegram_id`.
- Черновик прайса 11:00: inline «Утвердить» / «Отклонить» (`price:approve|reject:{draft_id}`) + команды fallback.
- Утверждать/отклонять: active employee **или** owner (без фейковой карточки employee); audit `approved_by_telegram_id`.
- `chat_role`: lookup `client_groups` по `chat_id` (исправлен bug с `session.get` по PK).

## 2026-09-09 — Наценки по линейкам

- `classify_category`: iPhone 17 Pro/Pro Max 800; остальные iPhone 500; AirPods/Watch/iPad 500; MacBook 800; Samsung 500, S26 Ultra 800; PlayStation/Dyson 800; `*` (чехлы, колонки) 500.
- `markup_rules` в prod заполнены теми же `markup_fixed`.

## 2026-09-09 — Черновик прайса всем owner с dm_ok

- `notify_morning_price_draft` шлёт draft в env-чаты approve **и** в ЛС `owners` с `dm_ok=true` (заместитель 260271772 после `/start`).
- Prod: третий owner уже в Supabase; тест `test_draft_also_notifies_dm_ok_owners`.

## 2026-09-09 — Morning-price в 11:00 МСК

- APScheduler `morning_price`: cron `0 11 * * *` Europe/Moscow (было 08:00).
- Причина: поставщики публикуют прайс в каналах 08:00–11:00; listener пишет в `tg_parser` сразу; backend забирает в `zakupki` после окна.
- Тест `test_scheduler.py`; спека §9.5, `СЕРВИСЫ.md`.

## 2026-09-09 — Проверка prod + контракт LLM в Coolify

### Production (проверка снаружи)

- `GET /health` backend: `{"status":"ok","db":"ok"}`.
- Webhook/jobs без секрета: `401`. `/docs` и `/openapi.json`: `404` (`DOCS_ENABLED=false`).
- Parser API снаружи недоступен (как задумано). Каналы прайса по последнему внутреннему smoke (2026-09-06) пустые.
- GitHub Actions `ci` на `main`: fail (ruff), pytest не доходит.

### LLM

- Ключи/провайдер пользователь добавил в Coolify env; публично это не верифицируется.
- Контракт кода: `LLM_PROVIDER` ∈ `openai` | `openai_compatible` | `openrouter` | `ollama`. DeepSeek → `openai_compatible` + `LLM_BASE_URL=https://api.deepseek.com`. `LLM_PROVIDER=deepseek` ломает вызовы.
- После правки env обязателен Restart `bot-saller`.

### Документация

- `СЕРВИСЫ.md`, `README.md`, `PHASE4_SETUP.md`, `.env.example`, `ИЗВЕСТНЫЕ_ПРОБЛЕМЫ.md`.

## 2026-09-06 — Production parser на Coolify + фиксы migrate

### Production (ops, не только код)

- Stack `tg-channel-parser` (`ssnr4auxpbo6jccgixbbmppd`): postgres/api/listener/worker Running, migrate exit 0.
- Predefined network: parser Advanced → Connect to predefined network; bot-saller уже в `coolify`.
- Smoke из backend: health `db: ok`, `/channels` 401 без токена, `[]` с токеном.
- Каналы прайса на тот момент не включены; LLM env появился позже (2026-09-09) — см. «Готовность» в `СЕРВИСЫ.md`.

### Код parser (`0002_channel_resolution`)

- `606d9ab` — не дропать UNIQUE `parser_channels.channel_id` (FK `parser_posts`).
- `83d4bca` — индекс `idx_parser_tasks_resolve_channel` без `WHERE task_type = 'resolve_channel'` в той же транзакции, что `ADD VALUE`.

### Документация

- `СЕРВИСЫ.md`: готовность, auth_cli placeholder + `--no-deps`, три секрета Telegram, hostname после redeploy, smoke без скрипта в образе backend.

## 2026-09-05 — Owner `/menu`: управление сотрудниками

### Backend

- `/menu` → раздел **Сотрудники**: список, добавление (имя + `telegram_id` или forward), карточка, вкл/выкл (`active`).
- `admin_service`: `list_employees`, `add_employee`, `toggle_employee_active`; дубликат `telegram_id` отклоняется (в т.ч. inactive).
- Роутинг owner DM: живой `AdminDialog` обрабатывается без обязательного `/` (fix шага ввода id в диалоге).
- Парсеры `telegram_id`: `parse_telegram_id_text`, `extract_forwarded_user_id` (`forward_from`, `forward_origin.user`).
- Тесты: `test_admin_menu.py`, `test_telegram_utils.py`.

### Документация

- Обновлены `Техническая документация  бот для закупок.md` (§5, §7.1, §11.1, §15, §18), `СЕРВИСЫ.md`, шаблон `/help`.

## 2026-09-05 — tg-channel-parser Coolify rollout (код + docs)

### Parser microservice

- Исправлен HTTP-контракт backend ↔ parser: JSON-массивы и paginated `{items, next_cursor}`.
- Worker: короткие DB-транзакции (claim/process/finalize), chunked SHA-256 медиа.
- Listener: hot-reload каналов без restart при пустом старте.
- API: cursor pagination `GET /posts`, DB-aware `/health`.
- Compose: без host ports, `tg-parser-migrate`, env через Coolify `${...}`.

### Документация

- Добавлен **`СЕРВИСЫ.md`** — production Coolify/VPS, env, сеть UUID-hostname, smoke.
- Обновлены `CLAUDE.md`, `AGENTS.md`, `PHASE4_SETUP.md`, `ИЗВЕСТНЫЕ_ПРОБЛЕМЫ.md`.
- Порт parser API: **8000** (исправлен устаревший 8100 в docs).

### Ops scripts

- `tg-channel-parser/scripts/coolify-bootstrap.sh`
- `tg-channel-parser/scripts/smoke-from-backend.sh`
- `tg-channel-parser/scripts/trigger-morning-price.sh`

## 2026-08-10 — n8n removal migration

### P0: Удаление n8n из runtime

- Telegram webhook переведён на прямую проверку `X-Telegram-Bot-Api-Secret-Token`.
- Плановые задачи переведены на in-process APScheduler внутри backend.
- `n8n` удалён из `docker-compose.yml`; workflow JSON оставлен только как архив для отката.

### P1: Документация и конфиг

- Обновлены `.env.example`, `CLAUDE.md`, `БЕЗОПАСНОСТЬ.md` и проектные инструкции под новый runtime.
- Добавлены тесты scheduler и обновлены webhook-тесты под новый заголовок.

## 2026-07-31 — Security/Resilience Hardening

### P0: Безопасность и целостность данных

- Закрыт IDOR для supplier reply: проверка `MessageOut(request_id, supplier_id)` перед привязкой ответа.
- Закрыт IDOR для employee-команд: доступ к заявке только в своей группе (или в личке владельцу заявки).
- Добавлены guard'ы статуса для `/setprice` и supplier reply (`closed/cancelled` блокируются).
- Включен constant-time compare для `X-Webhook-Secret` через `hmac.compare_digest`.
- Введена уникальность `quotes(request_id, supplier_id)` и атомарный upsert.
- Переведены денежные поля с `float` на `Decimal` в парсерах, LLM-схемах и сервисах сделок/квот.
- Усилен compose: обязательные секреты без fallback, порты только `127.0.0.1`.
- Обновлен `.env.example` новыми обязательными и эксплуатационными переменными.

### P1: Устойчивость и лимиты

- Добавлен Telegram throttle (`global + per-chat`) и retry/backoff на `429/5xx/transport`.
- Добавлен backstop rate limit middleware для `/telegram/webhook` и `/jobs/*`.
- Добавлен лимит размера HTTP body.
- Daily report теперь возвращает `sent/failed`, не падает целиком при сбое одного чата.
- Approve прайса теперь возвращает `approved_partial` при частичной публикации.
- Parser/LLM HTTP paths получили retry и лимит размера ответа.
- DB pool получил `pool_recycle` и `pool_timeout`.
- Recheck при ошибке переносится, чтобы не зациклить cron.

### P2: Качество, тесты, CI

- Введена Pydantic-модель webhook envelope.
- Добавлены новые тесты: supplier IDOR, group whitelist, inactive supplier, Telegram retry.
- Добавлены `pyproject.toml` (ruff/mypy), `.pre-commit-config.yaml`, `.github/workflows/ci.yml`.
- Зафиксированы версии зависимостей в `backend/requirements.txt`.
- Добавлен `deploy/nginx.conf.example` с rate limits и `client_max_body_size`.

## Миграции

- `0002_indexes_and_constraints`:
  - дедуп `quotes` по `(request_id, supplier_id)`;
  - unique constraint `uq_quotes_request_supplier`;
  - индексы hot-path для `requests`, `quotes`, `messages_out`, `raw_prices`, `parsed_items`.

## Операционные заметки

- После обновления нужно применить Alembic миграции: `alembic upgrade head`.
- Для продакшена обязательно вынести backend за reverse proxy и применить лимиты из `deploy/nginx.conf.example`.
