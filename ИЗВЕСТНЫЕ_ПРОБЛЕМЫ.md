# Известные проблемы и статус устранения

Последнее обновление: 2026-07-31.

## Статусы

- `fixed` — исправлено в коде.
- `accepted` — принято как текущее поведение.
- `deferred` — отложено в технический долг.

## Реестр

| ID | Severity | Компонент | Проблема | Статус |
|---|---|---|---|---|
| SEC-001 | high | `supplier_messages` | Поставщик мог привязать reply к чужой заявке по `#request_id` | fixed |
| SEC-002 | high | `employee_commands` | Сотрудник мог работать с чужой заявкой из другого group chat | fixed |
| SEC-003 | high | `whitelist` | `is_supplier` не учитывал `active` | fixed |
| SEC-004 | medium | `deps` | Сравнение webhook secret через `!=` вместо constant-time | fixed |
| SEC-005 | medium | `docker-compose.yml` | Порты БД/backend были открыты на все интерфейсы | fixed |
| SEC-006 | high | `docker-compose.yml` | Дефолтные секреты `changeme` в compose fallback | fixed |
| SEC-007 | medium | `request_service` | В логах светился `telegram_id` поставщика при ошибке отправки | fixed |
| SEC-008 | high | `owner_commands`/`jobs_router` | Отчеты уходили как HTML без дополнительной защиты | fixed |
| SEC-009 | medium | `report_fallback` | Jinja рендер без `autoescape` | fixed |
| REL-001 | high | `telegram/client` | Нет retry/backoff на 429/5xx | fixed |
| REL-002 | high | `telegram/client` | Нет throttle отправок в Telegram | fixed |
| REL-003 | high | `jobs_router` | Daily report падал частично при ошибке одного чата | fixed |
| REL-004 | critical | `price_service` | Approve прайса мог дать partial publish без явного статуса | fixed |
| REL-005 | high | `main` | Нет in-app backstop rate limit на webhook/jobs | fixed |
| REL-006 | medium | `main` | Нет ограничения размера request body | fixed |
| REL-007 | medium | `recheck_service` | Вечный recheck-loop при постоянной ошибке | fixed |
| REL-008 | medium | `parser_client` | Нет retry и нет лимита размера ответа | fixed |
| REL-009 | medium | `llm/client` | Нет retry и нет лимита размера ответа | fixed |
| REL-010 | medium | `db/session` | Pool без recycle/timeout | fixed |
| DATA-001 | high | `quote_service`/DB | Нет уникальности `quotes(request_id, supplier_id)` | fixed |
| DATA-002 | high | DB | Отсутствовали индексы hot-path (`requests`, `quotes`, `raw_prices`) | fixed |
| DATA-003 | medium | `quote_service` | Неатомарный upsert quote (race на дубликат) | fixed |
| DATA-004 | medium | `employee_commands`/`deal_service`/`bargain_service` | Деньги через `float` | fixed |
| DATA-005 | medium | `llm/schemas`/`regex_parser`/`nl_commands` | Цены в схемах и парсерах через `float` | fixed |
| DATA-006 | medium | `quote_service` | Переход `request -> priced` мог происходить без цены | fixed |
| DATA-007 | high | `employee_commands` | `/setprice` принимал closed/cancelled request | fixed |
| DATA-008 | high | `supplier_messages` | Ответ поставщика записывался и в closed/cancelled request | fixed |
| ARCH-001 | medium | `webhook_router` | Нет валидации структуры webhook body (`dict[str, Any]`) | fixed |
| ARCH-002 | medium | `webhook_router` | Нет ограничения длины входного текста | fixed |
| ARCH-003 | low | `messages_ru` | Мертвый шаблон `supplier_raw_forward` | fixed |
| ARCH-004 | low | `regex_parser` | Лишний wrapper `_extract_condition` | fixed |
| ARCH-005 | low | `config` | Неиспользуемый `backend_url` | fixed |
| ARCH-006 | medium | docs | Ссылка на `TECHNICAL_DOCUMENTATION.md` невалидна (файл в корне с другим именем) | fixed |
| ARCH-007 | medium | docs | В docs заявлен `tg-channel-parser`, но директории нет | fixed |
| TEST-001 | medium | tests | Нет теста на supplier IDOR | fixed |
| TEST-002 | medium | tests | Нет теста на inactive supplier behavior | fixed |
| TEST-003 | medium | tests | Нет теста на retry в Telegram client | fixed |
| TEST-004 | medium | tests | Нет теста на group whitelist блокировку | fixed |
| OPS-001 | medium | repo | Не было CI с lint/type/test | fixed |
| OPS-002 | medium | repo | Не было pre-commit + lint/type конфигов | fixed |
| OPS-003 | medium | parser microservice | `tg-channel-parser` не реализован в текущем репозитории | deferred |
| OPS-004 | low | orchestration | n8n удалён из runtime, workflow JSON сохранён только как архив | fixed |

## Открытые риски (после текущей итерации)

1. `deferred`: полноценный `outbox` для отделения внешних side-effects от транзакций во всех сервисах.
2. `deferred`: отдельный runtime для `tg-channel-parser` (документация есть, кода нет).
3. `deferred`: унификация всех командных шаблонов и бизнес-ошибок в отдельный слой ответов.
